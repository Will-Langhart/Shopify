
{
  "blog": {
    "title": "{{ blog.title | json }}",
    "articles": [
        {
          "title": "{{ article.title | json }}",
          "content": "{{ article.content | strip_html | escape | json }}"
    ]
  }
}
